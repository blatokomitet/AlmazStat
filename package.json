{
  "name": "almazstat",
  "version": "1.0.0",
  "description": "Telegram Mini App foundation for football analytics",
  "scripts": {
    "start": "node server.js",
    "prod": "NODE_ENV=production node server.js",
    "check": "node --check app.js && node --check server.js && node --check lib/sportmonks-provider.js && node --check lib/recent-form-service.js && node --check lib/gemini-analysis-provider.js",
    "test": "node --test",
    "check:sportmonks": "node scripts/check-sportmonks.js"
  },
  "private": true,
  "type": "module",
  "dependencies": {
    "express": "^5.2.1"
  }
}
